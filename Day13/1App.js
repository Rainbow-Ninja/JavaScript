//const elephant = require('./1Util'); //should call this students, not elephants!!
const userData = require('./1Util');
const {user, students} = userData; 

console.log(students);
console.log(user);
//console.log(elephant);